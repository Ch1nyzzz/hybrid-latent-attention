# GB128 / 200-step paired Stage3 and OPD restart

User-approved: restart both arms from Stage1-600, 8 GPUs each, GB128, 200 updates, checkpoint and full MATH500 every 10 updates. Stage3 M=2 ordinary backward; OPD K=3 with generated cache reuse.

Functional batches:
1. Medium risk: external evaluation mode and interval driver. Resume exact optimizer/RNG every 10 steps; release trainer before eight isolated vLLM shards. Validate argument planning, resume metadata, all 500 distinct IDs, no duplicate or missing samples, serving backend/capacity and aggregation.
2. High risk: OPD alignment/drift evidence. EOS async export alignment fix already qualified in v3. Five live full updates: mean logp error .00935–.00963, outside-clip fraction .000390–.000474; maximum .374–5.229. This is not evidence that the maximum difference disappeared. Preserve cache/trajectory validation and PPO correction. Add abort before optimizer for mean >.03 or outside-clip fraction >.01; these are operational budgets, not proofs of forward equality. Test failure paths and verify a real new GB128 update.
3. High risk: restart both jobs and scheduled evaluation. Stop old jobs, retain checkpoints, fresh initialization. Eight-GPU short serving smoke before training; full MATH500 at 10,20,...,200. Observe first actual updates; first full score is a separate milestone.

MATH500: full500, n=1, temperature1, top_p.7, max_new8192, max_model_len10240, seed20260915, 8 stable shards, TRITON_ATTN + FULL_DECODE_ONLY, no prefix/chunked-prefill/async scheduling, capacity-checked concurrency up to64 per GPU. Report accuracy/pass@1, mean generated tokens and truncation. Not directly identical to historical avg@4 protocol. The 8-problem/16-token smoke is not reported as a benchmark score.

No independent agents used; concentrated local review. Reuse unchanged successful tests; add only checks for new protocol/persistence/drift boundaries. Hash only the imported evaluation dataset and upload bundle for identity/transfer integrity. No git commit of unrelated dirty work or runtime artifacts.


## Launch and checks

Code asset `loop-s6-khop3-code-0919:13`, dependencies `:1`.
- Stage3: `2101231523964399616`, output `loop-s6-stage3-gb128-math200-0919`.
- OPD: `2101231569506140160`, output `loop-s6-opd-gb128-math200-0919`.
- Old jobs stopped: Stage3 `2101219382251225088`, OPD v3 `2101219845558239232`; retained existing checkpoints.
- Targeted verification: 49 tests passed across interval config/aggregation/drift/resume, M-unroll, direct trainer, MATH500, cache replay; one additional interval sequencing test passed (50 total). Shell syntax passed. No full-repo tests or agent reviews; no mixed Git commit.
- Dataset: copied the existing 500-unique-ID MATH500 file into the asset, identity recorded in provenance; upload transfer verified by bootstrap.
- Live runtime verification pending below; submission is not an optimizer update.


### Initial launch correction

Stage3 v13 failed before its first optimizer update. With external validation, the initial Teacher capture hooks no longer happened to be removed by validate(0); teacher scoring therefore retained an extra full set of activations across microbatches. OOM reached ~69GiB allocated plus ~8GiB reserved. Fix: immediately remove initial capture hooks after teacher load; only scoped scoring captures remain. Regression checks exactly one pair of attention hooks while scoring, plus uninterrupted/resumed equivalence. OPD v13 stopped during image initialization before training to apply the same fix. This is not addressed by reducing batch size or loosening a numerical check.

### Corrected launch (code asset :14)

- Stage3: `2101233620109754368`, output `loop-s6-stage3-gb128-math200-v2-0919`.
- OPD: `2101233657179013120`, output `loop-s6-opd-gb128-math200-v2-0919`.
- Final targeted verification after hook lifecycle fix: all 50 tests passed in 12.28s across the same five modules. No relevant code edits after this successful pass.
- Both fresh from Stage1-600; first optimizer update verification pending.

### Live OPD verification

All 8 ranks completed update 1. GB128 / 187333 response tokens; 262.515s inclusive update, objective .05453751, gradient norm .4377724; rollout 93.751s. Replay history collection zero. Mean logp error .00970366; max .621769; outside-clip fraction .000405695. Rank-local HF peak allocations 15.94–16.69GiB exclude the separate vLLM process. Update2 222.547s, objective .04774780, gradient norm .5444009; continuing. First full MATH500 score still pending update10.

Stage3 code14 initial attempt failed in inference-smoke shard2 before training (subprocess exited1; detailed file not directly readable through CLI after pod deletion). Retried same job as attempt1 with engine-log forwarding on errors; no training or inference numerical changes.

### Verified paired launch

Stage3 attempt1 passed all 8 inference smoke shards and completed update1 on all 8 ranks: GB128, M2, MB<=8/token cap16384, 243417 response tokens, 130.672s inclusive update, objective .04435531, gradient norm .5273509. Rank0 peak52.877GiB; no microbatch activation accumulation failure. Earlier smoke failure did not reproduce on retry; its root cause remains unconfirmed.

OPD completed 5 updates and entered update6. Updates2–5: 222.547,225.363,215.043,224.276 seconds (mean221.807s). Cache collection remains0. Both jobs continue to200; checkpoint/full500 n1 evaluation every10. No new checkpoint or full evaluation score yet at this observation.

### OPD step20 evaluation failure and recovery fix

Recovered worker5 engine log: vLLM EngineCore failed during distributed initialization with `DistNetworkError / EADDRINUSE`, TCP port39673. No generation began on that shard. Other7 shards completed. The unhandled subprocess error terminated the interval driver after checkpoint20; no evidence of training OOM or numerical failure in this incident.

Medium-risk runtime batch: assign per-GPU/per-attempt VLLM_PORT starting points (18000+1000*gpu+200*attempt); retry at most3 launches only for EADDRINUSE, log each attempt, reap subprocess groups, surface engine log tails. Other errors remain fatal. Add explicit mounted-checkpoint resume; evaluate checkpoint20 before resuming at21. Training/math protocol unchanged. Local focused review, no agents. 35 targeted tests passed (interval orchestration/retry/resume, direct trainer, MATH evaluation); unrelated full suite not repeated.

Recovery uses source job2101233657179013120, checkpoint2101254796735426560, original complete.json+training.pt. Platform marks that archive nonresumable, so a custom archive of the retained train directory was created through the member checkpoint API, and the exact checkpoint-000020 subdirectory is explicitly selected for restore. A slow local upload was canceled and is not used. Code asset :15. Full step20 evaluation will be regenerated under the same500/n1/seed/decoding protocol in the new output because prior output mount is not available to the new job.

Recovery launch: `2101346803520634880`, code15, native resume archive `2101346483780464640`, exact step20 payload SHA256 checked against the downloaded original before bootstrap. Original output model retained. No changes to Stage3.

2026-09-19 16:26 UTC: recovery job accepted, inputs prepared, pod Unschedulable (insufficient physical GPU/CPU/memory for an8-GPU node). No restored optimizer update yet. Platform will launch automatically when capacity is available. Source discovery rule restored to train/checkpoint- with complete.json marker after recovery archive creation.
