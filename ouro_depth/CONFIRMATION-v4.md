# V4 final DEV and independent confirmation

Use the single registered [PROTOCOL-v4.md](PROTOCOL-v4.md). The fixed4 and fixed8 final budgets are2400 and1200 updates, with shared work3,067,084,800. A middle checkpoint or a different inference exit cannot replace either final endpoint.

After both launch receipts report successful complete budgets, `confirm_v4.candidate_metadata(root)` validates final run/checkpoint/latest/plan/source/initializer bindings without opening or statting sealed test data. `validate_development(root, metadata)` binds all three saved DEV evaluations to the new complete1280-question data and the actual common initializer/final training receipts. The prediction-only `compare_v4_predictions.compare_prefixes(initializer, fixed4, fixed8, split='dev')` supplies the recorded decision and report. Save that report even if the gate fails.

If and only if the entire final DEV decision is eligible, `python -m ouro_depth.confirm_v4 --root ROOT` freezes the final candidates, source and prescribed evaluation commands in confirmation/v4-s20260915. Then `python -m ouro_depth.confirm_v4 --root ROOT --execute` revalidates the freeze, reloads all three models on DEV, requires every per-question discrete and numeric score to match, and only then scores the new sealed confirmation. Use the existing remote pinned environment and the allocated, verified GPU4/5. Run the controller as a recorded background process so the interactive task can continue monitoring it.

Any failed DEV condition leaves the confirmation set unscored. Any reload drift, stale binding, existing output or failed child produces a retained failed state without retry. Live children are reported and preserved for inspection. There is no override or automatic budget extension.

The test comparator reports five paired primary comparisons, adjusted exact McNemar p-values and paired intervals, plus own-exit learning and easy-task retention. Confirmed deep gain with a greater-than2pp loss atT8 is reported with that cost. One seed and synthetic pointer tasks do not establish natural-math transfer, all-depth stability or adaptive stopping.

Verification: six new mocked controller tests passed once, using the real prediction binding and comparator modules. The independent controller review and root combined review found no remaining blockers. No actual confirmation preparation or test scoring has occurred. Full GPU Adam reload is not repeated by this controller; trainer persistence evidence is recorded separately.
