# FKL learning-rate restart, 2026-09-19

User requested stopping the FKL experiment and rerunning with learning rate 3e-5.

- Old FKL job `2101366665114562560`: confirmed canceled; last completed update 29, loss 0.059718335, gradient norm 0.2578404546, LR 1e-6. Ready archived checkpoints 10 and 20 retained.
- New job `2101397650283704320`: fresh Stage1 `student-600.pt`, reset optimizer and step count. Output model `loop-s6-opd-fkl-lr3e5-gb128-math200-0919`.
- RKL job `2101346803520634880`: unchanged and confirmed running at submission.
- Code asset `loop-s6-khop3-code-0919:17`, derived from deployed version 16, with only the interval launcher's learning rate changed from 1e-6 to 3e-5. Source archive, patch, provenance, submission and idempotency key are under `artifacts/opd-fkl-lr3e5-20260919/`.

All other settings match the original FKL run: full-vocabulary KL(teacher || student) on student prefixes, no policy-gradient/PPO loss or attention auxiliary loss; 8 A100 80GB, GB128, 16 sequences per rank, replay MB1, K3 rollout-cache adjoints; prompt cap 1024 and response cap 2048; 200 updates, checkpoint and complete MATH500 n=1 every 10 updates. Initial inference smoke is not a task score.

## Verification and scope

This is a low-complexity configuration change with an increased optimization-instability risk due to the 30x learning rate. Self-review confirmed the archive's only source change is the LR literal. AST compilation and actual fresh/interval-resume argument checks passed, including full-vocabulary FKL selection. Transfer manifest integrity was checked on the imported package and regenerated for the uploaded artifact. No independent review agents or unrelated full-suite/build runs; unchanged loss/architecture tests were not duplicated. GPU startup and first real optimizer update are the remaining runtime acceptance checks.

The first submit attempt was rejected locally because the platform readback's Wenyon version_code=0 is not a valid CLI dataset version. Wenyon versions and the existing launcher confirmed corpus version 1; the corrected submission used that version. No training job was created by the rejected attempt.
