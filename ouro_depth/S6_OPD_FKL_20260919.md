# FKL OPD launch, 2026-09-19

The user requested stopping Stage3 and using its eight GPUs for an FKL OPD comparison.

- Stopped Stage3: `2101233620109754368`, confirmed canceled. Latest observed update: 180; loss 0.02731641, 129.595 s/update. Archived `checkpoint-000180`: `2101364616884924416`, ready before stopping.
- RKL comparison continues: `2101346803520634880` (resumed from step 20).
- New FKL job: `2101366665114562560`, output model `loop-s6-opd-fkl-gb128-math200-0919`.
- Source asset: `loop-s6-khop3-code-0919:16`, derived from deployed version 15, not the older root-workspace trainer. The exact source and diff are under `artifacts/opd-fkl-20260919/src/` and `fkl.patch`.
- Submit idempotency key: `98b3687d-0e11-4859-a664-2a48b8a4febc`.

## Matched configuration

8 A100 80GB, global batch 128, 16 student rollouts per rank, replay microbatch 1, K=3 cache-adjoint replay, BF16 serving numerics, LR 1e-6, AdamW, Stage1 student-600 initialization. Prompt limit 1024, response limit 2048, rollout T=1/top-p=1, 6 GiB KV per rank. 200 updates; save and full MATH500 n=1 every 10 updates, evaluation T=1/top-p=.7/max-new=8192.

The FKL loss is full-vocabulary KL(teacher || student) at stop-gradient student-generated prefixes, globally response-token normalized. No PPO/PG term and no attention auxiliary loss. Teacher targets use the frozen full-KV model on identical sampled prefixes. Existing rollout version checks, cache validation and pre-update drift checks remain: mean absolute logp error <= .03 and fraction outside [0.8,1.2] ratio <= .01.

This is a comparison of the existing sampled-token RKL/PPO objective with direct full-vocabulary FKL; it also changes the gradient estimator and PPO correction. It is not an isolated direction-only full-vocabulary KL ablation. K=3 is truncated cache-adjoint propagation. The first response prediction is a detached prefill constant counted in loss/token normalization, matching the baseline; no claim of full prefix or unlimited-history gradients.

## Verification

- Implementer reviewed the three-file diff against deployed v15; no review agents used.
- 15 targeted FKL/K-hop tests passed, with the upstream verl-only test deselected because the local pinned verl dependencies are unavailable. Initial testing caught and fixed a metadata compatibility issue.
- Additional FKL trainer test passed: two uninterrupted optimizer updates equal a one-update checkpoint plus resumed update, exactly in student parameters and metadata. Final FKL file: 5 tests passed.
- Covered explicit FKL direction and gradients against autograd, frozen teacher, first-token boundary, collected versus rollout-cache replay loss/gradient equality, behavior-logp drift, and interval CLI configuration.
- No unrelated build/full repository tests or duplicate successful checks. Transfer hashes verify the downloaded base artifact and uploaded package; ordinary source hashes were not used as a delivery gate.
- GPU startup and first real optimizer update are recorded below when observed. CPU numerical checks alone do not establish GPU memory feasibility or task improvement.

## Observed GPU acceptance

Job is running and began rollout version 1 after completing update 1. First real global update: 261.3178 s including rollout 93.7981 s; 187,333 response tokens; loss 0.05573745; grad norm 0.60333449; 0.580706 GPU-hours. Rank-0 teacher/replay were 2.8968/106.8564 s. Rank-0 trainer peak allocated memory was 16.0817 GiB (excludes separate vLLM processes and is not an all-rank device peak).

Drift checks passed: mean absolute logp error 0.00970366 <= .03; ratio outside [0.8,1.2] fraction 0.000405695 <= .01. Maximum absolute error 0.621769 is recorded but no maximum-error gate is configured, matching RKL. Full 2048-token response replay completed. This establishes one-update feasibility, not convergence or improved MATH500; first scheduled checkpoint/evaluation is step 10. The initial 8-problem, 16-token inference smoke is not a task score.
